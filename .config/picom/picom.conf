# Key things to enable/configure:
vsync = true;           # Prevent screen tearing
backend = "glx";        # Use OpenGL (faster)

# Transparency (optional)
inactive-opacity = 0.9;
active-opacity = 1.0;

# Fading
fading = true;
fade-delta = 5;

# Shadows (optional)
shadow = true;
